# Native-owner integration receipt for approved Stack moves

Status: **native integration verified; no new repository**.

The earlier recommendation to create `operations-capabilities` was wrong. It
optimized for repository cleanliness while recreating the fragmentation this
refactor is meant to remove. The approved `move` disposition now means:

> absorb the capability into the existing tool or workflow that already owns
> the job, verify native discovery, then remove the redundant Stack entry.

## Integration map

| Stack capability | Existing owner | Result |
|---|---|---|
| `arc-sidebar-guarded-migration` | Codex weekly Arc automation plus Hermes `idea-ingest` Arc reference | Existing equivalent verified; retire redundant Stack wrapper. |
| `david-online-shopping` | Hermes `personal-item-research` | Richer native equivalent verified; retire imported Stack wrapper. |
| `gbrain-zk-operating-system` | Hermes `brain-ops` and its Zettelkasten/GBrain-first reference | Operating levels, evidence order, validation, and closeout absorbed. |
| `gemini-zk-orchestrator` | Zettelkasten Gemini factory plans, scripts, board, and recovery runbook | Roadmap orchestration contract absorbed into the existing runbook. |
| `local-finance-interface` | Zouzou Finance Desk and monthly-close workflow | Local-interface authority, privacy, freshness, and no-mutation rules absorbed. |
| `mookie-bookmark-taxonomy` | Hermes `idea-ingest` | Taxonomy, adoption-candidate, and thin-source rules absorbed into Arc/bookmark ingestion. |
| `personal-file-organization-review` | Installed Codex skill and current Codex file-organization workflow | Byte-identical installed equivalent verified; retire duplicate Stack source. |
| `vault-migration-consumer-map` | Existing Zettelkasten split/reverse-split plans | Existing consumer inventory, cutover, rollback, privacy, and falsification gates verified. |
| `zettelkasten-plan-review-gate` | Hermes `writing-plans` plus Zettelkasten plan/review workflow | ZK-specific review gate absorbed as a native writing-plans reference. |

## Boundary

This integration creates no repository, product, or top-level stack. It does
not enable any scheduler, model call, source registration, embedding, Vault or
Fieldbook write, finance action, purchase, or runtime publication. After the
native consumer receipts were recorded, the redundant Stack sources were
removed and the 136-capability post-migration catalog/audit verified cleanly.
