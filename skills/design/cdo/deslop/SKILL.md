---
name: cdo-deslop
description: Deprecated compatibility source for deslop. Use the canonical deslop capability.
---

# Deprecated: CDO Deslop

The implementation was merged into `skills/deslop/`. Runtime publication must
provide `cdo-deslop` as a compatibility alias of `deslop`; do not maintain a
second implementation here.
