interface:
  display_name: "Matt Domain Modeling"
  short_description: "Build and sharpen a project's domain model. Use when the..."
  default_prompt: "Use $matt-domain-modeling to build and sharpen a project's domain model. Use when the user wants to pin down domain terminology or a ubiquitous language, record an architectural decision, or when another skill needs to maintain the domain model."
