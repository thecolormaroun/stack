interface:
  display_name: "Matt Wayfinder"
  short_description: "Plan a huge chunk of work — more than one agent session can..."
  default_prompt: "Use $matt-wayfinder to plan a huge chunk of work — more than one agent session can hold — as a shared map of investigation tickets on your issue tracker, and resolve them one at a time until the way to the destination is clear."
