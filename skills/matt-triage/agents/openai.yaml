interface:
  display_name: "Matt Triage"
  short_description: "Move issues and external PRs through a state machine of..."
  default_prompt: "Use $matt-triage to move issues and external PRs through a state machine of triage roles — categorise, verify, grill if needed, and write agent-ready briefs."
