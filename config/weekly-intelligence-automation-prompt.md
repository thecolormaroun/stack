Run Stack's approved fully automatic weekly design-intelligence loop from a dedicated clean automation checkout. The Codex automation remains anchored to the saved Stack project for identity and permissions, but do not execute collection or promotion from the user's potentially dirty primary checkout.

Use `$HOME/.local/share/stack/weekly-intelligence-source` as the execution checkout. If it does not exist, create it from the canonical `https://github.com/thecolormaroun/stack.git` origin, then set the checkout directory to owner-only mode `0700`. Before every run, require that it is an owner-local, owner-only, real directory with that exact origin, no tracked or untracked changes, and no `assume-unchanged` or `skip-worktree` index flags; fetch `origin/main` without tags, and check out the fetched full `origin/main` commit in detached mode. If creation, permissions, provenance, cleanliness, index, fetch, or checkout validation fails, finish `retry_with_alert`; never clean, reset, merge, or switch the user's saved Stack checkout. Run all commands below from the validated automation checkout. The live entrypoint independently rejects any other root and refetches `origin/main` before private collection; a stale-race result is a failed-closed retry, not permission to modify either checkout.

Start in a sanitized local environment and run `/opt/homebrew/bin/python3.11 scripts/run-stack-weekly-live.py` exactly once. Do not use the ambient `python3`; macOS may resolve it to Python 3.9. This is the private collection phase: it reads the current local Field Theory bookmark database, reconciles an owner-local snapshot, imports only missing `x-bookmarks` through GBrain with embeddings disabled, and runs the deterministic six-stage coordinator. Keep raw bookmark content, credentials, subprocess stderr, and private response payloads out of chat, Git, pull requests, and logs.

Do not stop at `awaiting_approval`. That legacy coordinator state now means the collection packet is ready for this approved automatic tail. Use the exact `live_binding_receipt_relative_path`, `live_binding_receipt_digest`, `campaign_receipt_relative_path`, and `campaign_receipt_digest` returned by the live entrypoint; never select a receipt merely because it is newest. Resolve both relative paths below `$HOME/.local/state/stack/weekly-intelligence`. Follow that bound campaign receipt to its design-packet, retrieval, and candidate-evaluation artifacts. If there is no fresh material evidence, finish as `no_action`. A quiet week is healthy and must not manufacture a candidate.

When material evidence exists, use Stack's design-intelligence instructions to produce a high-quality critique that separates visible facts, interpretation, reusable principles, suitable contexts, anti-patterns, accessibility, motion, responsive behavior, implementation cues, uncertainty, and citations. Prefer concrete visual or interaction evidence over category labels and generic design advice. Generate at most one candidate per run.

Before any model/reviewer wave, run `$HOME/Codex/scripts/codex-quota-preflight.sh` exactly once and obey its executor cap and allowed models. The material-evidence gate requires either two independent sources supporting the same reusable behavior, one source plus a repeated critique failure, or one source that fixes a documented hard evaluation failure. A candidate must target an existing Stack-owned `skills/**/SKILL.md` or `skills/**/references/**/*.md`, change no more than three files or 32,768 bytes, preserve provenance and license boundaries, and avoid vendor, imported, routing, command, runtime, credential, source-corpus, and private-overlay paths.

Encode the selected materiality basis in the candidate packet's `rationale.materiality` object with exactly `basis`, `source_count`, `critique_failure_ids`, and `evaluation_failure_ids`. `source_count` is the number of distinct `source_identity` values carried by the selected evidence citations, not the number of evidence IDs. Use basis `two-independent-sources`, `source-plus-repeated-critique-failure`, or `source-fixes-hard-evaluation-failure`; bind critique failures only to a persisted design-packet `cluster_id` whose two or more cards all carry an anti-pattern/failure record, and bind evaluation failures only to a persisted candidate-evaluation `hard_gate_failures[].failure_id`. Leave the non-applicable failure list empty. If the bound campaign artifacts do not contain the required source or failure records, that basis is unavailable. Both the materializer and terminal recorder independently reject a candidate that does not satisfy one of these shapes.

Extend `source_lineage` with the exact campaign run ID, campaign receipt digest, and the persisted artifact digests for `design_packet`, `retrieval`, and `candidate_evaluation`. The packet ID/digest, card IDs, revision IDs, evidence IDs, and selected change ID must come directly from those three bound artifacts. Do not invent new opaque lineage IDs in the automatic tail; the terminal recorder reopens the coordinator artifacts and verifies every ID and digest.

Materialize the smallest candidate in an isolated clean worktree based on current `origin/main`; never edit or clean the user's primary checkout. Create an owner-local authorization bound to the campaign run ID and campaign receipt digest with authorization contract `weekly-design-auto-promotion-approved-v1`, scope `isolated-owner-local-patch-only`, decision `approved`, and reviewer identity `weekly-design-automation`. Invoke `/opt/homebrew/bin/python3.11 scripts/materialize-capability-change.py --automatic-weekly-design --campaign-receipt <bound-coordinator-receipt> --campaign-receipt-digest <returned-campaign-receipt-digest>`; the ordinary materializer mode is not acceptable for this lane. This command must reopen the bound campaign artifacts and validate distinct source identities and every critique/evaluation failure ID before it writes any materialization output or permits PR creation. Bind the patch to its exact evidence, base commit, target files, and rollback digests. Run the `frozen-design-eval` gate against the reviewed harness at `$HOME/Codex`, requiring the candidate to beat `codex-current` on at least four fixed fixtures, pass the protected holdout and rotating canary, and have no structural, behavioral, visual, accessibility, privacy, citation, mobile-width, overflow, workflow, critical-data, or HTML hard failure. Then run the full Stack repository tests and obtain a fresh independent review whose final verdict is `ship`. Any implementation fix invalidates that verdict and requires another fresh review.

Only after every local gate passes, create or update one lineage-bound non-draft pull request from the isolated branch, wait for all required GitHub checks, and require a green pull-request-ci gate. Merge automatically only when the head SHA and candidate digest still match the reviewed inputs. Verify the merge on `origin/main`; a merely opened or auto-merge-queued pull request is not merge verification.

After verified merge, publish from a fresh clean checkout of the merged `origin/main` using `/opt/homebrew/bin/python3.11 scripts/bootstrap-stack.py --install --expected-source-commit <verified-origin-main-merge-commit>` with the standard owner-local deployment, staging, and receipt roots, then run `/opt/homebrew/bin/python3.11 scripts/stack-doctor.py`. Publication must use the canonical `https://github.com/thecolormaroun/stack.git` origin, refresh its remote `main` immediately before canonical catalog compilation and again immediately before pointer movement, and require clean source `HEAD`, refreshed `origin/main`, both stage source/tree attestations, and the supplied merge commit to agree exactly. Compile from an installer-owned 0700 detached snapshot of that commit into a unique owner-private staging transaction and seal the compiled stages read-only. Run each configured verifier only against a disposable copy in a dedicated process group, terminate the entire group on success or timeout, then make a fresh sealed publication copy and revalidate it immediately before linking, after linking, and before receipt persistence. Verifier errors and timeouts must preserve the immutable failed transaction receipts; any failure after a switch must roll back every switched pointer. The public installer must not accept caller-supplied target files or stages. Promotion proof must use the exact immutable transaction pair beneath the configured `~/.local/state/stack/runtime-receipts` root; an equivalent pair elsewhere is invalid. Require both runtime discovery plus the installer's rollback receipt before reporting `published`. Never install from the candidate branch or a dirty checkout.

Weak candidates finish `no_action`. Candidates that fail materiality, evaluation, tests, review, or policy finish `rejected_no_queue`; retain a small owner-local audit receipt but do not leave a human approval backlog or open pull request. Operational failures such as unavailable GitHub, eval harness, runtime publisher, or reviewer finish `retry_with_alert`, preserve resumable evidence, and retry on the next scheduled run without weakening a gate. Before recording either failure outcome, close the lineage pull request and disable auto-merge; an open pull request is never a resumable terminal state.

For every terminal outcome, write the exact decision contract as an owner-local mode-0600 JSON document below `$HOME/.local/state/stack/weekly-intelligence/promotion-decisions`, then run `/opt/homebrew/bin/python3.11 scripts/record-weekly-design-promotion.py --live-receipt <bound-live-receipt> --live-receipt-digest <returned-live-receipt-digest> --campaign-receipt <bound-coordinator-receipt> --decision <decision-json> --out-dir "$HOME/.local/state/stack/weekly-intelligence/promotion-receipts"`. The decision's `evidence` object must contain exactly these keys: `candidate_packet`, `materialization`, `evaluation`, `repository_tests`, `independent_review`, `pull_request_ci`, `merge_verification`, `runtime_publication`, and `rollback_receipt`. Each used value is `{ "path": <owner-local-0600-json>, "digest": <exact-file-sha256> }`; unused values are `null`. The test receipt must bind the candidate digest and reviewed head to the exact full-suite command and passing count. The review receipt must bind the same candidate and head to a fresh independent `ship` verdict. GitHub CI and merge receipts must bind the candidate, pull request, head, checks, merge commit, and `origin/main`. Runtime and rollback evidence must be the immutable `transactions/<transaction-id>/install.json` and `rollback.json` pair emitted by the atomic installer, with the same transaction ID and directory; mutable `latest.json` and `rollback-state.json` aliases are status pointers only and never publication proof. Do not report a terminal disposition unless this fail-closed recorder accepts it. A `published` receipt requires every named gate and every real evidence artifact. The terminal receipt retains only evidence digests, never raw private content or absolute source paths.

Do not enable Direct X/OAuth, paid-provider fallback, source-contract expansion, automatic upstream pin changes, destructive cleanup, or publication outside this exact Stack-owned skill/reference contract. Report a concise safe summary containing the collection outcome, candidate disposition, pull request and merge state when applicable, runtime publication state, rollback receipt state, and newest owner-local terminal receipt path.
